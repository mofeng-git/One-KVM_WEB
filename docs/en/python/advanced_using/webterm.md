### Notes

The web terminal logs in as `kvmd-webterm`. For security, this user cannot use `sudo`. To run root commands, execute `su root` to switch to the root account.

![image-20240622160417474](../../img/image-20240622160417474.png)